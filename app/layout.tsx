import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ICONOVOUS Holdings Limited | Enterprise IT & Digital Transformation",
  description: "ICONOVOUS Holdings Limited delivers enterprise-grade IT consulting, cloud infrastructure, DevOps, cybersecurity, software engineering, and AI automation solutions.",
  keywords: [
    "IT Consulting",
    "DevOps",
    "Cloud Infrastructure",
    "Cybersecurity",
    "AI Automation",
    "Software Development",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
